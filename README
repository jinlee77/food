This repository is for Tasly Anguo project.

To setup the development environment:
1. git clone git@github.wdf.sap.corp:DevFactory/anguo.git
2. copy hybris 5.6.0 into the root directory with name "hybris".
3. run ". setup.sh"
4. run ". createDB.sh", input root password of mysql when prompt to create anguo db.
5. run "cd hybris/bin/platform"
6. run "./hybrisserver.sh" to start hybris server.
